package com.example.recommend.ui.theme

import androidx.compose.ui.graphics.Color

/** App screen background — pure white */
val SoftPastelMint = Color(0xFFFFFFFF)

/** Primary text — near black */
val DarkPastelAnthracite = Color(0xFF1A1A1A)

/** Primary accent — charcoal (was coral) */
val RichPastelCoral = Color(0xFF2C2C2C)

/** Secondary — mid gray (was teal) */
val MutedPastelTeal = Color(0xFF6E6E6E)

/** Accent highlights — silver (was gold) */
val MutedPastelGold = Color(0xFF9E9E9E)

/** Secondary surfaces (nav, chips) */
val SurfacePastel = Color(0xFFFFFFFF)

/** Muted surface — for chips, secondary blocks */
val SurfaceMuted = Color(0xFFE8E8E8)

/** Inset panels (wallet, etc.) */
val GradientTop = Color(0xFFF5F5F5)
val GradientMid = Color(0xFFEBEBEB)
val GradientBottom = Color(0xFFF0F0F0)
